# React + Vite
# Hello React – Student List Assignment

This project is a simple React application created to display a list of students using components.  
It includes basic JSX, props, and CSS styling.

---

## 📁 Project Files Included
The main files for this assignment are:

- **App.jsx** – Parent component  
- **Profile.jsx** – Child component  
- **App.css** – Styling for components

These files are located inside the **src** folder.

---

## 🧩 What This Project Does
- Shows a list of students  
- Uses React components  
- Passes props to child components  
- Applies basic CSS styling  
- Demonstrates clean folder structure  

---

## ▶️ How to Run the Project
Make sure Node.js is installed, then run:

```bash
npm install
npm run dev

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh
